\documentclass[11pt]{article}
\usepackage[paperwidth=180mm,paperheight=100mm,margin=5mm]{geometry}
\pagestyle{empty}
\usepackage{amssymb}
\usepackage{tikz}
\usetikzlibrary{positioning,arrows.meta,calc}

\begin{document}
\begin{tikzpicture}[
  node distance=15mm and 14mm,
  every node/.style={font=\normalsize},
  box/.style={draw, rounded corners=4pt, align=center, inner sep=8pt,
              fill=gray!6, text width=34mm, minimum height=13mm},
  core/.style={draw, rounded corners=4pt, align=center, inner sep=8pt,
               fill=gray!18, thick, text width=62mm, minimum height=13mm},
  steplbl/.style={font=\small, align=center, fill=white, inner sep=2.5pt,
                  draw=gray!25, rounded corners=2pt, line width=0.3pt},
  flow/.style={-{Stealth[length=2.6mm,width=1.8mm]}, draw=gray!65, line width=0.6pt}
]

\node[box] (sma) {\textbf{SMA}\\[1pt]\small Safe $\cdot$ holds assets $\cdot$ executes};
\node[box, above=15mm of sma] (owner) {\textbf{Owner}\\[1pt]\small holds the Safe $\cdot$ signs the mandate};
\node[box, left=26mm of sma] (manager) {\textbf{Manager}\\[1pt]\small agent $\cdot$ signs dispatches};
\node[box, right=26mm of sma] (mandate) {\textbf{Mandate}\\[1pt]\small set of permission contracts};
\node[core, below=16mm of sma] (kernel) {\textbf{Sail Kernel}\\[1pt]\small evaluates permission $\cdot$ trusted core\\[1pt]\small dispatches to Safe on success};

\draw[flow] (owner) -- node[steplbl, pos=0.55] {\textbf{01} deploys \& owns} (sma);
\draw[flow] (owner.south west) -- node[steplbl, pos=0.5] {\textbf{03} appoints\\[-1pt]\small instant revocation} (manager.north);
\draw[flow] (owner.south east) -- node[steplbl, pos=0.5] {\textbf{02} signs mandate\\[-1pt]\small EIP-712} (mandate.north);
\draw[flow] (manager.south) -- node[steplbl, pos=0.5] {\textbf{04} signs dispatch\\[-1pt]\small EIP-712} (kernel.north west);
\draw[flow] (mandate.south) -- node[steplbl, pos=0.5] {\textbf{05} defines bounds} (kernel.north east);
\draw[flow] (kernel.north) -- node[steplbl, pos=0.5] {\textbf{06} $\checkmark$ executes on SMA\\[-1pt]\small $\oslash$ outside mandate: reverts} (sma.south);

\end{tikzpicture}
\end{document}
